# Render deployment — call the hosted backend directly.
# NOTE: requires the backend to allow CORS from this UI's origin.
VITE_API_BASE_URL=https://mini-wsa-pipeline.onrender.com

# Default live-refresh interval for polling views, in milliseconds.
VITE_POLL_INTERVAL_MS=10000
